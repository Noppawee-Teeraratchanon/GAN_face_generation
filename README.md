# GAN_face_generation


This GAN model was trained for a total of 50 epochs.
The repository includes the saved generator weights from the final epoch, along with an example output image generated at epoch 50.